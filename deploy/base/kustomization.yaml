# Observe-only: the controller maintains the allowlist, nothing enforces it.
namespace: adsblol
resources:
- rbac.yaml
- deployment.yaml
- servicemonitor.yaml
- gatewayclassconfig.yaml
commonLabels:
  app: reapi-allowlist
